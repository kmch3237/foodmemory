# food_collection (mealmates)

Spring Boot · Thymeleaf · MySQL. 먹은 것을 사진으로 기록하고 방(space)에서 함께 모으는 서비스.

## 기능을 구현할 때 — 접근 순서를 먼저 말한다

**코드를 건드리기 전에, 어떻게 접근할지 짧게 말하고 시작한다.** 3~6줄이면 된다.
사용자가 나중에 혼자 기능을 만들 때 이 순서가 머릿속에 그려지는 것이 목적이다.

순서는 늘 이렇다:

1. **무엇을 만드는지 한 문장으로.** 여기서 흐리면 코드로 못 간다. 흐리면 묻는다.
2. **이미 있는 것부터 찾는다.** 새로 짜기 전에 비슷한 기능이 있는지 본다.
   있으면 그 결을 따른다. 이 저장소에 이미 있는 방식이 정답에 가깝다.
3. **바꿀 곳과 안 바꿀 곳을 정한다.** 파일 이름을 대며 말한다.
4. **가장 작은 것부터 하나씩.** 화면부터인지 데이터부터인지 정하고 한 번에 하나만 바꾼다.
5. **깨질 만한 곳을 미리 짚는다.** 페이징 순서, 캐시, 기존에 쌓인 데이터,
   무한 스크롤로 이어붙는 부분 — 이 프로젝트에서 실제로 어긋났던 곳들이다.
6. **어떻게 확인할지 정해둔다.** 무엇을 보면 됐다고 할 수 있는지 먼저 정한다.

다 끝난 뒤에 "이렇게 했습니다" 로 설명하지 않는다. **시작할 때 말하고, 그 순서대로 간다.**
중간에 순서를 바꿔야 하면 왜 바꾸는지 한 줄 말한다. 그 판단이 사용자가 배울 부분이다.

## 설명 방식

- 새 용어가 나오면 **그 자리에서** 한 줄로 뜻을 밝힌다. 아는 것을 전제하지 않는다.
- "왜 필요한가(어떤 문제를 푸는가)" 를 먼저, "무엇인가" 는 그다음.
- 명령어를 시킬 때는 그 명령이 무엇을 하는지 한 줄 붙인다.
- 돈이 드는지, 위험한지, 되돌릴 수 있는지에 미리 답한다.

설명이 안 통했으면 `/wait-what`, 개념을 남길 만하면 `/library`,
하루를 정리할 때는 `/daily`.

## 알고 시작할 것

- **Spring Security 가 없다.** 인증은 직접 만들었다.
  `LoginCheckInterceptor`(출입 검사) + `LoginArgumentResolver`(`@Login` 으로 주입) + `SessionConst`.
  지킬 주소 목록은 `WebConfig` 에 있다. 새 화면을 만들면 여기에 넣어야 한다.
- **세션은 Redis 에 있다.** 앱을 재시작해도 로그인이 유지된다. 로컬에도 Redis 가 떠 있어야 앱이 뜬다.
- **`ddl-auto: validate`** — JPA 가 테이블을 만들지 않는다. 엔티티를 바꾸면 DDL 을 직접 쓴다.
- **정적 파일에는 내용 해시가 붙는다**(`/css/app-<해시>.css`). 고치면 주소가 바뀌어 캐시가 저절로 갈린다.
- **운영은 `prod` 프로파일**로 뜬다(`application-prod.yml`). 쿠키 secure·캐시·SQL 로그가 로컬과 다르다.

## 돌려보고 확인하기

- 앱은 **사용자가 직접 띄운다.** 로컬에 MySQL 과 Redis 가 둘 다 떠 있어야 뜬다.
- **`src/main/resources` 를 고쳤으면 `./gradlew processResources` 를 돌린다.**
  안 돌리면 `build` 에 옛 파일이 남아 화면이 그대로다. 템플릿·CSS 를 고치고
  "왜 안 바뀌지" 하는 일이 실제로 두 번 있었다.
- 로그인이 필요한 화면은 curl 로도 볼 수 있다. Redis 의 세션 ID 를 쿠키로 쓴다.

  ```
  SID=$(redis-cli keys 'spring:session:sessions:*' | head -1 | sed 's/.*sessions://')
  curl -H "Cookie: SESSION=$(printf %s "$SID" | base64)" http://localhost:8080/
  ```

  사용자에게 매번 확인을 부탁하기 전에 이걸로 먼저 본다.

## 배포

- **main 에 push 하면 곧바로 배포된다**(GitHub Actions → EC2, `systemctl restart foodmemory`).
  push 는 배포와 같은 말이다.
- **push 가 조용히 실패할 수 있다.** 네트워크가 끊기면 실패하는데 그대로 넘어가기 쉽다.
  `git log origin/main..main` 이 비었는지 반드시 확인하고 "배포했다" 고 말한다.
- **새 의존성이 서버 프로그램을 필요로 하면 EC2 에 먼저 깔아야 한다.**
  안 깔고 push 하면 앱이 못 떠서 배포가 실패한다(Redis 때 실제로 겪을 뻔했다).

## 자주 어긋나는 지점

- **무한 스크롤** — 첫 화면과 `/posts/more` 의 정렬·범위가 같아야 한다.
  다르면 본 사진이 또 나오거나 중간이 빠진다. 정렬을 추가할 때마다 이 둘을 같이 고친다.
- **페이징 정렬에는 겹치지 않는 값을 뒤에 붙인다**(`postId`).
  같은 값이 여럿이면 페이지마다 순서가 달라진다.
- **모바일 규칙은 `app.css` 뒤쪽 `@media (max-width: 640px)` 에 몰려 있다.**
  PC 규칙이 폰에서만 뒤집히면 여기부터 본다.

## 이 저장소에서 주의할 것

- **8080 포트 프로세스를 죽이지 않는다.** 사용자가 자기 터미널에서 `bootRun` 을 직접 띄워둔다.
- 커밋은 사용자가 읽어보고 직접 한다. 시키지 않으면 커밋하지 않는다.
- 커밋 메시지 본문에 **왜 그렇게 했는지**를 담는다. 이 저장소의 커밋은 그 자체가 기록물이다.
